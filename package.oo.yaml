version: 0.0.1
scripts:
  # Script executed when the container is first loaded
  # eg: bootstrap: poetry add pandas
  bootstrap: |
    npm install
    poetry install --no-root
icon: ./icon.png
displayName: "%key1%"
description: "%transform-your-data-into-insights-without-writing-a-single-line%"
